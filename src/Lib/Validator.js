const Validator = {
  validateUserCache(userCache) {
    // TODO UserCahe에 대한 조건 체크, 통과X시 throw 던지기
  },

  validateWinningNumbers(winningNumbers) {
    // TODO WinningNumbers에 대한 조건 체크, 통과X시 throw 던지기
  },

  validateBonusNumber(bonusNumber) {
    // TODO bonusNumber에 대한 조건 체크, 통과X시 throw 던지기
  },
};

module.exports = Validator;
